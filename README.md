An appstack is a packaged application stack with canned configuration,
suitable for deploying in a common manner across a range of Tribblix
environments, although the initial implementation is as a custom zone.

The current implementation is strictly a prototype, to validate the
concepts and tooling.
